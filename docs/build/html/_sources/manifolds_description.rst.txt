Manifolds
=========

* Complex Stiefel manifold.

Complex Stiefel manifold :math:`{\rm St}(n, m)` is the following set
.. math::

	{\rm St}(n, m) = \{Z \in C^{n\times m}|Z^\dagger Z = I\},

i.e. set of all isometric matrices of size :math:`n \times m`.